<%= turbo_stream.update 'modal-root' do %>
  <%= render partial: 'form_modal',
             layout: 'components/dialog',
             locals: { title: t('badges.edit.title'), url: badge_path(@badge) } %>
<% end %>
