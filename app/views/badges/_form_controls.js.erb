$('input[name="badge[achieved_at]"]')
  .datepicker({
    format: 'dd.mm.yyyy',
    startDate: 0,
    language: I18n.locale,
    autoclose: true,
    todayHighlight: true
  });

Skyderby.helpers.ProfileSelect($('select[name="badge[profile_id]"]'));

$('select[name="badge[category]"]').select2({
  theme: 'bootstrap',
  width: '100%',
  minimumResultsForSearch: 10
});
