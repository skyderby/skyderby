$('input[name="badge[achieved_at]"]')
  .datepicker({
    format: 'dd.mm.yyyy',
    startDate: 0,
    language: I18n.locale,
    autoclose: true,
    todayHighlight: true
  });

$('select[name="badge[category]"]').select2({
  theme: 'bootstrap',
  width: '100%',
  minimumResultsForSearch: 10
});
