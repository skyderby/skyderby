<%# locals: (page:, frame_id:, term:) %>

<turbo-frame
  id="<%= frame_id %>"
  src="<%= url_for(page:, frame_id:, term:) %>"
  data-controller="replace-with-children"
  data-action="turbo:frame-render->replace-with-children#trigger"
  loading="lazy"
>
  Loading...
</turbo-frame>
