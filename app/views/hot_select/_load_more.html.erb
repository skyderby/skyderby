<%# locals: (page:, frame_id:) %>

<turbo-frame
  id="<%= frame_id %>"
  src="<%= url_for(params.to_unsafe_hash.merge(page:, frame_id:)) %>"
  data-controller="replace-with-children"
  data-parent-selector=".hot-select-options"
  data-action="turbo:frame-render->replace-with-children#trigger"
  loading="lazy"
>
  Loading...
</turbo-frame>
