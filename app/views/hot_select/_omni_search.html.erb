<%# locals: (options: [], **attrs) %>

<%= tag.div 'data-controller': 'omni-search', class: 'form-input hot-select-container hot-select-control', **attrs do %>
  <template data-omni-search-target="tagTemplate">
    <%= render 'hot_select/tag' %>
  </template>

  <div class="omni-search-container">
    <div class="selected-tags-container" data-omni-search-target="tagsContainer">
      <% options.each do |value, name, param, label| %>
        <%= render 'hot_select/tag', type: label, param:, name:, value: %>
      <% end %>
    </div>

    <div class="hot-select-placeholder"
         data-action="click->omni-search#toggle"
         data-hot-select-target="placeholder">
      <%= attrs[:placeholder] || raw('&nbsp;') %>
    </div>
  </div>
  <div class="hot-select-indicators">
    <button type="button" class="hot-select-button" data-action="omni-search#clear">
      <%= icon_tag 'xmark' %>
    </button>
  </div>

  <%= yield %>
<% end %>
