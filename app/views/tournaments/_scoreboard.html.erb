.row.tournament-scoreboard
  - rounds = @tournament.rounds.order(:order)
  - if rounds.size < 4
    .col-lg-1
  - rounds.each do |round|
    = render partial: 'tournaments/rounds/round', locals: { round: round }


