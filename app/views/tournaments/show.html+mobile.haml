- provide :title, @tournament.name

.page-wrap
  = render 'tournaments/header'
  = render 'tournaments/navbar'

  - if @tournament.surprise?
    %h1.text-muted.text-center
      🙅 Let it be a Surprise
  - else
    .content-block
      .scoreboard
        .scoreboard-scroll
          = render 'scoreboard'
