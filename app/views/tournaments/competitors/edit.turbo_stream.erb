<%= turbo_stream.update 'modal-root' do %>
  <%= render partial: 'form_modal', layout: 'components/dialog', locals: { title: "#{t('activerecord.models.tournament_competitor')}: #{@competitor.name}" } %>
<% end %>
