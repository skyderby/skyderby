<% editable = @event.editable? && !@event.finished? && !@result.round.completed? %>

<%= render partial: 'jump_range_form',
           layout: 'performance_competitions/results/layout',
           locals: {
             result: @result,
             editable:
           } %>
