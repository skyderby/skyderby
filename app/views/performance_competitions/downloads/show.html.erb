<div class="show-page">
  <%= render 'performance_competitions/header', editable: @event.editable? %>

  <div class="card card-sm">
    <div class="downloads-row">
      <div class="downloads-title">
        <div>Scoreboard (by category)</div>
        <% @event.categories.each do |section| %>
          <div><%= section.name %> - <code><%= "skyderby-performance-#{@event.id}-#{section.id}" %></code></div>
        <% end %>
      </div>
      <%= link_to 'ISC XML', performance_competition_downloads_scoreboard_path(@event, format: :xml), class: 'button', download: true %>
    </div>
    <div class="downloads-row">
      <div class="downloads-title">
        <div>Open-Event Scoreboard</div>
        <code><%= "skyderby-performance-open-#{@event.id}" %></code>
      </div>
      <%= link_to 'ISC XML', performance_competition_downloads_open_event_scoreboard_path(@event, format: :xml), class: 'button', download: true %>
    </div>
    <div class="downloads-row">
      <div class="downloads-title">
        <div>Scoreboard (by task)</div>
        <% @event.rounds.pluck(:discipline).uniq.each do |task| %>
          <div><%= t("disciplines.#{task}") %> - <code><%= "skyderby-performance-#{task}-#{@event.id}" %></code></div>
        <% end %>
      </div>
      <%= link_to 'ISC XML', performance_competition_downloads_task_scoreboard_path(@event, format: :xml), class: 'button', download: true %>
    </div>
    <% if @event.use_teams %>
      <div class="downloads-row">
        <div class="downloads-title">
          <div>Team standings</div>
          <code><%= "skyderby-performance-#{@event.id}-teams" %></code>
        </div>
        <%= link_to 'ISC XML', performance_competition_downloads_team_standings_path(@event, format: :xml), class: 'button', download: true %>
      </div>
    <% end %>
    <div class="downloads-row">
      <div class="downloads-title">Excel sheet</div>
      <%= link_to 'Excel sheet', performance_competition_downloads_scoreboard_path(@event, format: :xlsx), class: 'button', download: true %>
    </div>
    <div class="downloads-row">
      <div class="downloads-title">GPS Recordings</div>
      <%= link_to 'ZIP Archive', performance_competition_downloads_gps_recordings_path(@event), class: 'button', download: true %>
    </div>
  </div>
</div>
