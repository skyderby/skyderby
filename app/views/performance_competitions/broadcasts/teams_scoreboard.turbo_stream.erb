<%# locals: (event:, editable:, wind_cancellation:) %>
<% target = ['teams-scoreboard', ('no_wind' if wind_cancellation)].compact.join('_') %>

<%= turbo_stream.replace target, method: :morph do %>
  <%= render 'performance_competitions/teams/scoreboard', event:, editable:, wind_cancellation: %>
<% end %>
