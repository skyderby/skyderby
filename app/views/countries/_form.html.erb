<%= form_for(@country) do |f| %>
  <% if @country.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@country.errors.count, "error") %>
        <%= 'prohibited this country from being saved:' %>
      </h2>
      <ul>
        <% @country.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-row">
    <%= f.label :name, class: 'col-sm-2 form-label', for: :name %>
    <div class="col-sm-5">
      <%= f.text_field :name, autofocus: true, class: 'form-input' %>
    </div>
  </div>

  <div class="form-row">
    <%= f.label :code, class: 'col-sm-2 form-label', for: :code %>
    <div class="col-sm-5">
      <%= f.text_field :code, class: 'form-input' %>
    </div>
  </div>

  <div class="form-row">
    <div class="col-sm-offset-2 col-sm-8">
      <%= f.submit t('general.save'), class: 'button button--primary' %>
    </div>
  </div>
<% end %>
