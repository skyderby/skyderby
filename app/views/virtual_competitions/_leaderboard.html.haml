:ruby
  orientation ||= 'horizontal'
  orientation_class = "online-competition-leaderboard--#{orientation}"

.online-competition-leaderboard{class: orientation_class}
  - leaders.each do |leader_result|
    = render 'winner', result: leader_result
