= form_for @video, url: track_video_path, remote: true, html: {class: 'form-horizontal'} do |f|
  .row
    .col-sm-4
      %h3
        Youtube video
      %p
        Enter YouTube link here

    .col-sm-8.top-buffer
      .url-group
        = f.text_field :url, class: 'form-control'

      = f.hidden_field :video_code
      = f.hidden_field :track_id

  %hr

  .row
    .col-sm-4 
      %h3
        Start of the jump on video
      %p
        Play video until jump started, pause the video at the beginning of the jump,
        then press button 'Set' in the field to paste current time.
        Value in the field is a seconds from beginning of video.
    .col-sm-8
      #player

    .col-sm-3.col-sm-offset-4.top-buffer
      .input-group
        = f.text_field :video_offset, class: 'form-control text-center'
        %span.input-group-btn
          %button#set-current-time.btn.btn-default
            Set

  %hr

  .row
    .col-sm-4
      %h3
        Start of the jump on track
      %p
        Find start of the jump on the chart and click on it. 
        For precious changing use buttons in the field.
        Value in the field is a seconds from when you start recording jump.
    .col-sm-8
      #chart
    .col-sm-3.col-sm-offset-4
      .input-group
        %span.input-group-btn
          %button#dec-track-offset.btn.btn-default
            %i.fa.fa-chevron-left

        = f.text_field :track_offset, class: 'form-control text-center'

        %span.input-group-btn
          %button#inc-track-offset.btn.btn-default
            %i.fa.fa-chevron-right

  .row
    .top-buffer-30
      .col-sm-4.col-sm-offset-4
        = f.submit t('general.save'), class: 'btn btn-primary btn-lg'

      .col-sm-4.text-right
        = link_to t('general.delete'),
                  track_video_path,
                  method: :delete,
                  data: {confirm: 'Are you sure you want to delete this video?'},
                  class: 'btn btn-default'
