$('#tracks-index').append('<%= j render(@tracks) %>)');
<% if @tracks.next_page %>
    $('.pagination').replaceWith('<%= j will_paginate(@tracks) %>');
<% else %>
    $('.pagination').remove();
<% end %>
