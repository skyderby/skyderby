document
  .querySelector('.accounts__table > .tbody')
  .innerHTML = '<%= j render partial: 'manage/accounts/account', collection: @accounts, as: :account %>';

document
  .querySelector('.accounts__pagination')
  .innerHTML = '<%= j will_paginate(@accounts, renderer: BootstrapPagination::Rails) %>';
