<% provide :title, @competition_series.name %>

<div class="page-wrap">
  <div class="event__header">
    <div class="event__title"><%= @competition_series.name %></div>
  </div>

  <% if @competition_series.surprise? %>
    <h1 class="text-muted text-center">
      🙅 Let it be a Surprise
    </h1>
  <% else %>
    <%= render @scoreboard %>
  <% end %>
</div>

