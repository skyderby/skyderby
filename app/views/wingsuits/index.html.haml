.container
  .page-header
    %h1= 'Suits'

  - if can? :create, :wingsuit
    = link_to 'New', new_wingsuit_path, class: 'btn btn-default'
  
  - @wingsuits.each do |manufacturer, suits|
    .top-buffer
      .row
        .col-sm-12.col-md-12
          %h1.light-header= manufacturer ? manufacturer.name : '<...>'
        .col-sm-12.col-md-6
          %h2.light-header Wingsuits
          %ul.suits-list
            - suits.select(&:wingsuit?).each do |wingsuit|
              %li.suit-li
                .row
                  .col-sm-6
                    %a{href: wingsuit_path(wingsuit)}= wingsuit.name
                  .col-sm-2.text-muted
                    .text-center
                      = wingsuit.pilots.group_by(&:id).count
                    .text-center
                      pilots
                  .col-sm-2.text-muted
                    .text-center
                      = wingsuit.tracks.public_track.base.count
                    .text-center
                      base
                  .col-sm-2.text-muted
                    .text-center
                      = wingsuit.tracks.public_track.skydive.count
                    .text-center
                      skydive
              -# - if can? :update, wingsuit
              -#   = link_to t('.edit', default: t("helpers.links.edit")),
              -#                 edit_wingsuit_path(wingsuit), class: 'btn btn-default btn-xs'
              -#   = link_to t('.destroy', default: t("helpers.links.destroy")),
              -#                 wingsuit_path(wingsuit),
              -#                 method: :delete,
              -#                 data: { confirm: t('.confirm', default: t("helpers.links.confirm", default: 'Are you sure?')) },
              -#                 class: 'btn btn-xs btn-danger'
        .col-sm-12.col-md-6
          %h2.light-header Tracksuits
          %ul.suits-list
            - suits.select(&:tracksuit?).each do |tracksuit|
              %li.suit-li
                .row
                  .col-sm-6
                    %a{href: wingsuit_path(tracksuit)}= tracksuit.name
                  .col-sm-2.text-muted
                    .text-center
                      = tracksuit.pilots.group_by(&:id).count
                    .text-center
                      pilots
                  .col-sm-2.text-muted
                    .text-center
                      = tracksuit.tracks.public_track.base.count
                    .text-center
                      base
                  .col-sm-2.text-muted
                    .text-center
                      = tracksuit.tracks.public_track.skydive.count
                    .text-center
                      skydive

    %hr
