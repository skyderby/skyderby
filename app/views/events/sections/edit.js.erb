$('#modal').html('<%= j render('form_modal') %>');

$('#modal').one('shown.bs.modal', function() {
  $('input[name="section[name]"]').focus();
});

$('#modal').modal('show');

<%= render 'form_validation', form_id: "##{dom_id(@section, :edit)}" %>
