.container.top-buffer

  - if current_user
    = link_to(new_event_path, remote: true, class: 'btn btn-default btn-info') do
      %i.fa.fa-plus
      = 'Competition'
  - else
    %span= 'Creating classic competitions available only for registered users.'
  .top-buffer
  .content-block
    %ul.comp-list
      - @events.each do |event_entity|
        = render event_entity.event

  .text-center.top-buffer
    .pagination-list
      = will_paginate @events, renderer: BootstrapPagination::Rails
