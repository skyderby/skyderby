<% editable = policy(@event).update? && !@event.finished? && !@result.round.completed? %>

<%= render partial: 'map',
           layout: 'events/results/layout',
           locals: {
             result: @result,
             editable:
           } %>
