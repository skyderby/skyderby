<% editable = policy(@event).update? && !@event.finished? && !@result.round.completed? %>

<%= turbo_stream.update 'modal-root' do %>
  <%= render partial: 'events/results/result',
             layout: 'components/modal',
             locals: {
               title: event_result_presentation(@result),
               modal_size: 'lg',
               result: @result,
               modal: true,
               editable:
             } %>
<% end %>
