Skyderby.helpers.ProfileSelect($('select[name="competitor[profile_id]"]'));
Skyderby.helpers.SuitSelect($('select[name="competitor[wingsuit_id]"]'));
Skyderby.helpers.CountrySelect($('select[name="competitor[profile_attributes][country_id]"]'));

$('select[name="competitor[section_id]"]').select2({
  theme: 'bootstrap',
  width: '100%',
  minimumResultsForSearch: 10
});
