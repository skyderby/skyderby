Skyderby.helpers.ProfileSelect($('select[name="profile_id"]'));
Skyderby.helpers.SuitSelect($('select[name="suit_id"]'));
Skyderby.helpers.CountrySelect($('select[name="country_id"]'));

$('select[name="section_id"]').select2({
  theme: 'bootstrap',
  width: '100%',
  minimumResultsForSearch: 10
});
