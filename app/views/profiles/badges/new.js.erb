$('#modal').html('<%= j render('badges/form_modal', form_model: [@profile, @badge]) %>');
<%= render 'badges/form_controls' %>

$('#modal').one('shown.bs.modal', function() {
  $('input[name="badge[name]"]').focus();
});

$('#modal').modal('show');
