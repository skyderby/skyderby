<%= turbo_stream.update 'modal-root' do %>
  <%= render partial: 'badges/form_modal', layout: 'components/dialog', locals: { title: t('badges.new.title'), dialog_size: 'md', model: @badge, url: profile_badges_path(@profile) } %>
<% end %>
