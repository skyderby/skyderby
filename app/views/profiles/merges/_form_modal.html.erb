<%= form_with model: nil, scope: :profiles_merge, url: profile_merge_path(@destination_profile),
              data: { controller: 'profiles--merges-form modal-form', action: 'form:submit-success->modal#close' } do |f| %>
  <div class="sd-form">
    <div class="grid">
      <%= f.label :source_profile_id, class: 'form-label col-4' %>
      <%= render 'hot_select/select', form: f, name: :source_profile_id, src: profiles_select_options_path(include_ids: true), options: [], class: 'col-8' %>
    </div>

    <div class="help-text">
      <%= t '.description' %>
    </div>

    <div class="form-actions">
      <%= f.submit t('general.save'), class: 'primary-button' %>
      <button type="button" class="default-button" data-action="modal#close"><%= t 'general.cancel' %></button>
    </div>
  </div>
<% end %>
