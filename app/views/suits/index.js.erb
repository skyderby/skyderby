<% if @suits.overview? %>
  <%= render 'overview_chart' %>
<% else %>
  <%= render 'manufacturer_view' %>
<% end %>

window.history.replaceState(
  {},
  document.title,
  "<%= url_for(index_params).html_safe %>"
);
