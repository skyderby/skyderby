fr:
  activerecord:
    models:
      round: Tour
    errors:
      models:
        round:
          attributes:
            base:
              restrict_dependent_destroy:
                many: Pour supprimer la manche veuillez supprimer d'abord les résultats
              event_finished: Les modifications ne sont plus possibles car la compétition est finie
