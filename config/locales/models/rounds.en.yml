en:
  activerecord:
    models:
      round: Round
    errors:
      models:
        round:
          attributes:
            base:
              restrict_dependent_destroy:
                many: Delete round results first
              event_finished: Changes couldn't be made because event is finished.
